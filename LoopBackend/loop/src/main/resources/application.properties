spring.application.name=loop
# MySQL connection for LOOP
spring.datasource.url=jdbc:mysql://localhost:3306/loops
spring.datasource.username=root
spring.datasource.password=rootVidhya@123
# spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver  # usually optional with Boot 3

# JPA / Hibernate
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQLDialect
spring.jpa.show-sql=true
spring.jpa.hibernate.ddl-auto=update

# Frontend origin for CORS (React + Vite)
loop.frontend.origin=http://localhost:5173

# JWT
jwt.secret=change-this-secret-key-for-loop
jwt.expiration-ms=86400000

